select current_database(); 
select version();
select 'Hello SQL';



NOTES
* You can create a database with the command CREATE DATABASE (database name);
* Semicolons are used to end a SQL command, its not used ALL the time but its good practice to stick with
* To create a table its CREATE TABLE and in parenthesis after that each line is formed by a Data Type ending with a semicolon
* Two examples of data types you might use would be Varchar and Numeric, as varchar is a text column data type with a max of 50 characters. And the numeric type hold the static numbers
